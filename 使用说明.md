## Powkiddy A20 搞机工具箱

适用于 Powkiddy A20 (Amlogic sm1_ac202, Android 9 userdebug) 的一站式维护工具。

### 功能一览

**Magisk 管理**

| 编号 | 功能 | 说明 |
|------|------|------|
| 1.1 | 一键修复 Magisk | 刷写 patched boot + 修复 env 分区 + 重启验证，Magisk 升级后首选 |
| 1.2 | 刷写 Magisk boot | 仅刷写 boot 分区，适用于 boot 被还原的情况 |
| 1.3 | 修复 env 分区 | 仅修复 env 中 skip_initramfs → want_initramfs，适用于 U-Boot 回滚 |

**HDMI 脚本**

| 编号 | 功能 | 说明 |
|------|------|------|
| 2.1 | 安装 HDMI 脚本 | 安装 HDMI 自动分辨率切换脚本到设备（部署于 /vendor，自动清理旧版 /system 残留）|
| 2.2 | 卸载 HDMI 脚本 | 卸载 HDMI 脚本并恢复默认分辨率 |

**手柄修复**

| 编号 | 功能 | 说明 |
|------|------|------|
| 4.1 | 安装手柄修复 | 按键重映射(kl) + 摇杆行程修复(uinput 守护进程)，Moonlight/模拟器使用标准手柄 |
| 4.2 | 卸载手柄修复 | 移除守护进程与 init 服务，恢复按键映射备份 |

**恢复原厂**

| 编号 | 功能 | 说明 |
|------|------|------|
| 3.1 | 恢复原厂 boot | 刷回原厂 boot 镜像，Magisk 将失效 |
| 3.2 | 恢复原厂 su | 将原厂 su 写回 /system/xbin/su |
| 3.3 | 完全恢复原厂 | boot + su + env 全部还原，彻底移除 Magisk |

### 使用方法

1. USB 连接 Powkiddy A20，确认 ADB 调试已开启
2. 双击 `启动工具.bat`
3. 按二级菜单提示选择操作

### Magisk 升级后的修复流程

升级 Magisk 后，如果重启发现 Magisk 失效：

1. 运行本工具
2. 选择 **1. Magisk 管理** → **1. 一键修复 Magisk**
3. 等待自动修复完成

原理：Magisk 升级会重写 boot 分区，可能触发 U-Boot env 回滚。一键修复同时处理 boot 和 env。

### 注意事项

- **不要**在 Magisk App 中点击"修复运行环境"，会导致 env 被 U-Boot 回滚
- boot 刷写前会自动备份当前 boot 到 `backup/` 目录
- 如需更新 patched boot 镜像，将新的 Magisk patched boot 替换 `files/magisk_boot.img`
- env 分区 CRC 覆盖 65532 字节数据（非标准 64KB），这是 Amlogic U-Boot 的特殊行为
- `启动工具.bat` 保持纯 ASCII，所有中文文本通过 `zh.ps1` (PowerShell UTF-8 BOM) 输出，避免 cmd.exe 编码乱码

### 文件结构

```
powkiddy_a20_toolkit/
├── 启动工具.bat               ← 主菜单入口 (纯 ASCII)
├── zh.ps1                     ← 菜单中文文本 (UTF-8 BOM)
├── 使用说明.md
├── platform-tools/            ← ADB 工具
├── modules/
│   ├── Common.psm1            ← 公共模块 (ADB/CRC32/日志)
│   ├── Repair-Magisk.ps1      ← 一键修复 (boot+env)
│   ├── Install-Magisk.ps1     ← 刷写 Magisk boot
│   ├── Fix-MagiskEnv.ps1      ← 修复 env
│   ├── Restore-FactoryBoot.ps1← 恢复原厂 boot
│   ├── Restore-FactorySu.ps1  ← 恢复原厂 su
│   ├── Full-Restore.ps1       ← 完全恢复原厂
│   ├── HdmiMonitor.ps1        ← HDMI 脚本管理
│   └── GamepadFix.ps1         ← 标准手柄修复 (按键+摇杆)
├── files/
│   ├── magisk_boot.img        ← Magisk patched boot (16MB)
│   ├── factory_boot.img       ← 原厂 boot 镜像 (16MB)
│   ├── factory_su.bin         ← 原厂 su 二进制 (100KB)
│   ├── hdmi_monitor.sh        ← HDMI 监控脚本
│   ├── hdmi_monitor.rc        ← init 服务配置
│   ├── Vendor_0001_Product_0001.kl ← 手柄按键重映射 (扫描码→BUTTON_*)
│   ├── Vendor_0001_Product_0001.kl.orig ← 原厂按键映射 (卸载时还原用)
│   ├── a20_gamepad_daemon     ← 摇杆修复守护进程 (ARM32 静态)
│   ├── a20_gamepad.sh         ← 守护进程启动脚本
│   ├── a20_gamepad.rc         ← init 服务配置
│   ├── a20_gamepad_restart.sh ← 开机组合拳脚本（重建虚拟设备+重启霸王宝盒）
│   └── a20_gamepad_restart.rc ← 开机组合拳 oneshot 服务
└── backup/                    ← boot 备份 (自动生成)
```

### 技术背景

**HDMI 自动分辨率切换脚本做了什么？**

Powkiddy A20 自带屏幕是 640x480，接 HDMI 后系统原生切换逻辑不够完善。这个脚本作为 init 服务开机自启，持续监控 HDMI 热插拔状态 (`/sys/class/amhdmitx/amhdmitx0/hpd_state`)：

- HDMI 接入时：自动将渲染分辨率设为 1280x720 / 213dpi，静音本机扬声器（音频走 HDMI），10 秒后自动关闭本机屏幕省电
- HDMI 断开时：恢复 640x480 / 160dpi，恢复本机扬声器和屏幕亮度
- 持续监控：HDMI 连接期间如果本机屏幕意外亮起，会自动再次关闭

**手柄修复做了什么？**

A20 的按键(`gpio_keypad`)和摇杆(`joy_keypad`)是两个独立物理设备，且摇杆驱动声明的量程(±70)与实际值(±155)不符，导致 Android 框架归一化爆表(2.2)，Moonlight 串流时摇杆"推到底失灵/轻推爆表/方向随机"。修复分两部分：

1. **按键重映射**：kl 写入 `/vendor/usr/keylayout/`，把物理扫描码映射为标准手柄键（F1→L2、F2→R2、F3→Y、F4→R1、F5→A、F6→B、F7→X、F8→L1、F9→SELECT、ENTER→START、HOME→MODE，方向键标准）
2. **摇杆修复**：`a20_gamepad_daemon` 守护进程读物理轴 raw 值，按实测量程 `(raw-1)*32767/155` 归一化到标准 ±32767，合成一个干净统一的虚拟手柄 "A20 Gamepad"（按键+摇杆合并），并经 `a20_gamepad.rc` init 服务开机自启、grab 物理设备先于 Moonlight 抢占

**为什么部署到 /vendor 而不是 /system？**

本机 `/` 与 `/vendor` 都是真实 ext4 分区，`adb remount` / `mount -o remount,rw` 后写入都**持久**（hdmi_monitor.sh 部署于 /system，跨月重启保留可证）。**唯一例外**：当 Magisk 模块含 `system/` 目录时会挂载 overlay 挡住 /system 写入（写报只读且重启还原）。手柄修复当时正撞上模块 overlay 干扰，且按键 kl 原本就在 /vendor，故统一落 /vendor——同样持久（已跨重启验证）。

**为什么 Magisk 不持久？**

U-Boot 的 `storeboot` 命令在 SAR (System-As-Root) 模式下向内核 bootargs 注入 `skip_initramfs`，导致内核跳过 ramdisk。Magisk 的修补数据在 ramdisk 中，被跳过后 Magisk 完全不加载。

**修复方法**

将 env 分区 `storeboot` 变量中的 `skip_initramfs` 替换为 `want_initramfs`，并重新计算 CRC32。这样 U-Boot 不会注入 skip 指令，内核正常加载 ramdisk，Magisk 得以运行。
